import { useDispatch,useSelector } from "react-redux";

const useAppDispatch = useDispatch; //for dispatch
const useAppSelector = useSelector; //for selection

export {useAppDispatch, useAppSelector};